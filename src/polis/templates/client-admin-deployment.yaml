apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: polis
  name: polis-client-admin
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: polis
    spec:
      containers:
      - image: polisdemo/client-admin:dev
        imagePullPolicy: Always
        name: polis-client-admin
      restartPolicy: Always
