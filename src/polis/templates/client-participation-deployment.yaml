apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: polis-client-participation
  name: polis-client-participation
spec:
  replicas: 1
  selector:
    matchLabels:
      app: polis-client-participation
  template:
    metadata:
      labels:
        app: polis-client-participation
    spec:
      containers:
      - image: polisdemo/client-participation:dev
        imagePullPolicy: Always
        name: polis-client-participation
      restartPolicy: Always
