apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: polis
  name: polis-client-report
spec:
  replicas: 1
  selector:
    matchLabels:
      app: polis
  template:
    metadata:
      labels:
        app: polis
    spec:
      containers:
      - image: polisdemo/client-report:dev
        imagePullPolicy: Always
        name: polis-client-report
      restartPolicy: Always
